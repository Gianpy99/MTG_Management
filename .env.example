# Copia questo file in ".env" e compila i valori reali (mai committare .env).

# Cartella dati SQLite dentro il container (montata come volume in compose).
MTG_DATA_DIR=/app/data

# Ambiente app
APP_ENV=production
